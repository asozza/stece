- base.link:
    target: "{{job.amipfr.exe}}"
    link: "amip-reader.exe"

- base.link:
    target: "{{experiment.amipfr.forcing.dir}}/{{item}}"
    link: "{{item}}"
  loop:
    - "{{experiment.amipfr.forcing.sst}}"
    - "{{experiment.amipfr.forcing.sic}}"

- base.copy:
    src: "{{se.cli.cwd}}/templates/amipfr/namelist.amipfr.j2"
    dst: templates/namelist.amipfr.j2
